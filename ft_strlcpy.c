size_t(char *dst, const char *src, size_t dstsize)
{
	size_t srclen;

	srclen = ft_strlen(src);
	if (srclen + 1 < dstsize)
		ddddd;
	else if (srclen > dstsize - 1)


	/*si src > dst, mientras dstsize -1, dst = src. después, añadir la terminacion nula
